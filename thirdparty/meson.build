# thirdparty dependencies
#============================================================================

# nix build file
#============================================================================

nix_src_inc = [include_directories('.')]

src_dirs = [
  # 'nlohmann',
  'toml11',
]

foreach dir : src_dirs
    subdir(dir)
endforeach

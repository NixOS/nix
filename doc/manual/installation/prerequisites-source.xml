<section xmlns="http://docbook.org/ns/docbook"
      xmlns:xlink="http://www.w3.org/1999/xlink"
      xmlns:xi="http://www.w3.org/2001/XInclude"
      version="5.0"
      xml:id="sec-prerequisites-source">

<title>Prerequisites</title>

<itemizedlist>

  <listitem><para>GNU Make.</para></listitem>
  
  <listitem><para>Bash Shell (used by the <literal>./configure</literal> script)</para></listitem>

  <listitem><para>A version of GCC or Clang that supports C++14.</para></listitem>

  <listitem><para><literal>pkg-config</literal> (locates
  dependencies): <link
  xlink:href="http://www.freedesktop.org/wiki/Software/pkg-config"
  /></para></listitem>

  <listitem><para>OpenSSL (calculates cryptographic hashes): <link
  xlink:href="https://www.openssl.org"/>.</para></listitem>

  <listitem><para><literal>libbrotlienc</literal>
  <literal>libbrotlidec</literal> (Brotli compression algorithm): <link
  xlink:href="https://github.com/google/brotli" />.</para></listitem>

  <listitem><para><literal>bzip2</literal> (compressor program) and
  <literal>libbz2</literal>, including development headers and libraries: <link
  xlink:href="https://web.archive.org/web/20180624184756/http://www.bzip.org/"
  />.</para></listitem>

  <listitem><para><literal>liblzma</literal> in XZ Utils: <link xlink:href="https://tukaani.org/xz/"/>.</para></listitem>
  
  <listitem><para>cURL and its library:<link
  xlink:href="https://curl.haxx.se/"/>.</para></listitem>
      
  <listitem><para>SQLite embedded database library, version 3.6.19
  or higher:<link xlink:href="http://www.sqlite.org/" />.</para></listitem>

  <listitem><para>(optional)<link
  xlink:href="http://www.hboehm.info/gc/">Boehm
  garbage collector</link> (reduces the evaluator’s memory
  consumption). To enable it, install <literal>pkgconfig</literal> and the Boehm garbage collector, and pass the flag <option>--enable-gc</option> to
  <command>configure</command>.</para></listitem>

  <listitem><para><literal>boost</literal> version
  1.66.0 or higher: <link xlink:href="https://www.boost.org/" />.</para></listitem>

  <listitem><para>If you modify the manual sources or when you are building from the Git repository, you need <literal>xmllint</literal> <literal>xsltproc</literal> (part of <literal>libxml2</literal> and <literal>libxslt</literal> respectively), the <link
  xlink:href="http://docbook.sourceforge.net/projects/xsl/">DocBook
  XSL stylesheets</link>, and optionally the <link
  xlink:href="http://www.docbook.org/schemas/5x"> DocBook 5.0 RELAX NG
  schemas</link>.</para></listitem>

  <listitem><para>If you modify the parser or when you are building from the Git repository, you need Bison version 2.6: <link
  xlink:href="ftp://alpha.gnu.org/pub/gnu/bison">GNU FTP
  server</link>.</para></listitem>

  <listitem><para>If you modify the parser or when you are building from the Git repository, you need Flex version 2.5.35: <link
  xlink:href="http://lex.sourceforge.net/">SourceForge</link>.</para></listitem>

  <listitem><para>(optional) <literal>libseccomp</literal> (provides syscall filtering on Linux): <link xlink:href="https://github.com/seccomp/libseccomp" />. To disable this dependency, pass the <option>--disable-seccomp-sandboxing</option> option to the <command>configure</command> script (Not recommended unless your system doesn't support
  <literal>libseccomp</literal>).</para></listitem>

</itemizedlist>

</section>

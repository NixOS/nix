# nix systemd build file
#============================================================================

systemv_data = []

# install_data(
#     systemv_data,
#     install_dir : join_paths(prefix, 'lib/systemd/system'))

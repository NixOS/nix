# nix src build file
#============================================================================

misc_dirs = [
  'launchd',
  'systemd',
  'systemv',
  'upstart'
]

foreach dir : misc_dirs
    subdir(dir)
endforeach

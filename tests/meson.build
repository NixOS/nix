# nix tests
#============================================================================

test_dirs = [
    'nix',
    # 'funtional',
    # 'installer',
    # 'nixos'
    ]

foreach dir : test_dirs
    subdir(dir)
endforeach

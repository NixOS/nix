# nix unit tests
#============================================================================

unit_test_dirs = [
    'libutil',
    'libstore',
    # 'libexpr'
    ]

foreach dir : unit_test_dirs
    subdir(dir)
endforeach

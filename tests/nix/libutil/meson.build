# libutil unit tests
#============================================================================

libutil_test_data_dir = join_paths(meson.current_source_dir(), 'data')

libutil_unit_test_dirs = [
    # 'data',
    'support',
    'unit']

foreach dir : libutil_unit_test_dirs
    subdir(dir)
endforeach

warning: function comparison is unreliable; it may return true or false depending on structure and evaluation order (lint-function-comparison)
         at /pwd/lang/eval-okay-lint-function-comparison-warn-neq.nix:2:8:
              1| # The != operator also goes through eqValues, so the lint fires here too.
              2| (x: x) != (x: x)
               |        ^
              3|

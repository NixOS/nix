# nix-build build file
#============================================================================

nix_build_dir = meson.current_source_dir()
nix_build_inc = [include_directories('.')]


# src files
#============================================================================

nix_build_src = files(
    join_paths(nix_build_dir, 'nix-build.cc'))

R""(

# Examples

* Run the daemon:

  ```console
  # nix store roots-daemon
  ```

# Description

This command runs a daemon that serves roots from a socket.
It is not required in all nix installations, but it may be
useful when running with a daemon not running as root


# Systemd socket activation
`nix store roots-daemon` supports systemd socket-based activation.
)""

# nix-copy-closure build file
#============================================================================

nix_copy_closure_dir = meson.current_source_dir()
nix_copy_closure_inc = [include_directories('.')]


# src files
#============================================================================

nix_copy_closure_src = files(
    join_paths(nix_copy_closure_dir, 'nix-copy-closure.cc'))

# nix-instantiate build file
#============================================================================

nix_instantiate_dir = meson.current_source_dir()
nix_instantiate_inc = [include_directories('.')]


# src files
#============================================================================

nix_instantiate_src = files(
    join_paths(nix_instantiate_dir, 'nix-instantiate.cc'))

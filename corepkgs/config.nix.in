let
  fromEnv = var: def:
    let val = builtins.getEnv var; in
    if val != "" then val else def;
in {
  perl = "@perl@";
  shell = "@shell@";
  coreutils = "@coreutils@";
  bzip2 = "@bzip2@";
  xz = "@xz@";
  tar = "@tar@";
  tr = "@tr@";
  nixBinDir = fromEnv "NIX_BIN_DIR" "@bindir@";
}

# libstore.builtins build file
#============================================================================

libstore_inc += include_directories('.')


# src files
#============================================================================

libstore_src += files(
    'buildenv.cc',
    'fetchurl.cc',
    'unpack-channel.cc')


libstore_headers += files(
    'buildenv.hh')

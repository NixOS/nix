# nix build file
#============================================================================

nix_inc = [include_directories('.')]

nixbin_dirs = [
    'data',
    'misc',
    'src']

foreach dir : nixbin_dirs
    subdir(dir)
endforeach

# nix misc
#============================================================================

nix_inc += include_directories('.')

nix_misc_dirs = [
    'get-env',
    'generated-doc',
    'generate-manpage',
    'generate-settings',
    'generate-store-info',
    'utils']

foreach dir : nix_misc_dirs
    subdir(dir)
endforeach
